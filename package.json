{
  "name": "vocab-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:local": "vite build --mode preview --outDir dist-local && node scripts/create-local-readme.mjs",
    "build:tauri": "vite build --mode tauri",
    "preview": "vite preview",
    "preview:local": "serve dist-local -s",
    "build:data": "node scripts/build-data.mjs",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/plugin-shell": "^2.3.5",
    "echarts": "^6.1.0",
    "pinia": "^2.2.4",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/cli": "^2.11.2",
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.5"
  }
}
